# QED-Net 2.0 — backend requirements (Python 3.11+)
# Core ML / quantum / science stack. Versions are resolved at install time;
# exact reproducibility records are stored per experiment in
# experiments/runs/<id>/result.json (reproducibility.package_versions).

numpy>=1.26
scipy>=1.11
scikit-learn>=1.4
pandas>=2.1
xgboost>=2.0
lightgbm>=4.0
shap>=0.44
pennylane>=0.36
pennylane-lightning>=0.36
autograd>=1.6  # reverse-mode autodiff for the batched statevector simulator
imbalanced-learn>=0.12
pandera>=0.18
pyyaml>=6.0
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
celery>=5.3
redis>=5.0
mlflow>=2.11
hypothesis>=6.98
pytest>=8.0
httpx>=0.27
matplotlib>=3.8
